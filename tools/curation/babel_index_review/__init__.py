"""Story review GUI for the babel-index tile project.

Run from the repo root::

    python -m babel_index_review DIR

See ``babel_index_review.gui`` for the interface and ``babel_index_review.core``
for the shared tile/metadata/Claude logic (also used by
``babel_index_review.tile_process``).
"""
