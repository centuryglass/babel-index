node_modules/
*.log
.DS_Store
*.kate-swp

# Python (tools/curation).
__pycache__/
*.py[cod]

# Screenshots the browser smoke test leaves behind.
packages/web/e2e/artifacts/

# Blender source file (too large for git; kept local only) and its
# texture cache/backups.
reference/blender/babel_shelf.blend
reference/blender/textures
reference/blender/assets

# Blender's per-save backup files.
*.blend1

# Local tuning overlay (packages/config). Optional and never committed.
config.json

*.env
